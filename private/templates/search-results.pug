html
head
style(type="text/css").
  table, th, tr, td {
    min-width: 250px;
    min-height: 100px;
    border: 1px solid black;
    padding: 7px;
    border-collapse: collapse;
  }
title Search Results

body
h2 Search Results 


if (nResults == 0)
  h4 No results
else
  table
    tr
      each key in keys
          th= key
    each val in vals
      tr
        each v in val
          td= v 
